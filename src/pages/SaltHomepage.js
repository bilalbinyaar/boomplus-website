import React from 'react';
import SaltNav from '../components/salt/SaltNav';

const SaltHomepage = () => {
  return (
    <React.Fragment>
      <SaltNav />
    </React.Fragment>
  );
};

export default SaltHomepage;
